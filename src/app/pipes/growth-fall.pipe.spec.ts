import { GrowthFallPipe } from './growth-fall.pipe';

describe('GrowthFallPipe', () => {
  it('create an instance', () => {
    const pipe = new GrowthFallPipe();
    expect(pipe).toBeTruthy();
  });
});
